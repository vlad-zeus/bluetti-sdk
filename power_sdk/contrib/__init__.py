"""power_sdk.contrib — vendor-specific convenience helpers.

This package contains batteries-included helpers that import from plugin packages.
It is intentionally separate from the core SDK so that the core remains
vendor/protocol-neutral.
"""
